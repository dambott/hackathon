pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo 'Starting Hackathon Build'
        cd distributed-tracing/go-app
        #sh 'go build -o target/beachshirts  cmd/beachshirts/main.go'
        echo 'Pretend it worked!'
      }
    }

  }
}
